#!/usr/bin/env ruby
$LOAD_PATH.unshift File.expand_path('../../lib', __FILE__)

require 'yaml'
require 'ostruct'
require 'bosh/cpi'
require 'pry'

job_dir = ENV['BOSH_JOBS_DIR'] || "/var/vcap/jobs"

# binding.pry
input = STDIN.read
puts `echo "#{input}" | #{job_dir}/aws_cpi/bin/cpi`
