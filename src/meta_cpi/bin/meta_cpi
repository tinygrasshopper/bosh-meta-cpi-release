#!/usr/bin/env ruby
$LOAD_PATH.unshift File.expand_path('../../lib', __FILE__)

require 'yaml'
require 'ostruct'
require 'bosh/cpi'
require 'cloud/aws'

input = STDIN.read
puts `echo {input} >> /tmp/meta.log`
puts `echo {input} | /var/vcap/jobs/meta_cpi/bin/cpi`
