#!/bin/bash

set -e

RUN_DIR=/var/vcap/sys/run/meta_cpi
PID_FILE=${RUN_DIR}/cpi.pid

case $1 in

  start)
    mkdir -p $RUN_DIR
    echo 1 > $PID_FILE
  ;;

  stop)
    rm -f $PID_FILE
  ;;

esac
exit 0
